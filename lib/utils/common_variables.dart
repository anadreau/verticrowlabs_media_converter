///Enum for conversion status
enum Status { notStarted, inProgress, done, error }

///Enum for Resolution quality
enum MediaScale { low, medium, high }

///ENum for Container type
enum MediaContainerType { mp4, mkv, mov, avi, flv }
